Clue provides an interface to the Bunch cluster algorithm.
We have class files for bunch but no source.  Thus this
source acts as the interface between what can be compiled
and what cannot be compiled.

